# GR00T N1.5 진행 보고서 — FT 4차 반복 끝에 **근본 원인 규명: covariate shift**

> 작성일: 2026-07-20 | 환경구축→v3→v2 변환→FT v1~v4 반복 진단 **완료**
> 프로젝트: SO-ARM101로 4모델(ACT/SmolVLA/π0/GR00T N1.5) 비교. 본 문서는 네 번째 모델
> **GR00T N1.5**의 환경 구축(다수 함정 해결)과, 파인튜닝-진단-수정 4회 반복 끝에
> **실패 원인이 모델·학습설정이 아니라 데이터의 성격(교정 시연 부재)임을 규명**한 전 과정을 기록한다.
> **pi0에서 확립한 사전점검·진단 도구가 그대로 재사용되어 반복 주기가 크게 단축**됐다.

---

## 1. 버전 선정과 환경 구축 (5090, `~/gr00t_remote/`)

- **N1.5 선택** (계획 문서 준수, HF 블로그·커뮤니티 레퍼런스 최다). 단 Isaac-GR00T main은
  N1.7 전용(Qwen3-VL 백본)으로 **아키텍처가 달라 N1.5 체크포인트가 로드 불가** → `n1d5`
  브랜치 별도 clone. pi0 1차 실패(버전 불일치)와 같은 함정을 사전 차단.
- **Blackwell 대응**: n1d5 기본 torch 2.5.1은 sm_120 미지원 → **torch 2.8.0+cu128 오버라이드**
  + flash-attn 2.8.3 사전빌드 휠(cp310/torch2.8). venv 설치 검증 완료.
- 기타 함정: pyproject의 aarch64 torchcodec 경로휠(git-LFS 포인터)로 uv 해석 실패 →
  x86_64 전용화. 호스트 ffmpeg 부재 → 변환은 imageio-ffmpeg 정적 바이너리, 학습은
  `--video-backend torchvision_av`(pyav 내장)로 우회.

## 2. 데이터 준비 (LeRobot v3 → GR00T v2)

- GR00T는 LeRobot **v2** 요구 → 공식 `convert_v3_to_v2.py`로 변환 (도구 전용 uv env,
  `--root`는 상위 디렉터리 지정 주의, 로컬 v3 원본은 무손상 유지)
- **modality.json**: `front ← observation.images.top`, `wrist ← wrist` 매핑 + 6-dim
  single_arm/gripper 슬라이싱 — **pi0 #1 실패(카메라 키)를 정식 메커니즘으로 해결**
- 변환본 stats.json의 `count`([1])가 GR00T 차원 슬라이싱에서 IndexError → `fix_stats_for_gr00t.py`로 제거

## 3. 파인튜닝 반복 진단 (T1)

| 버전 | 구성 | loss | 실기 결과 | 진단 |
|---|---|---|---|---|
| v1 `gr00t_t1` | 기본(백본 freeze, projector+DiT 학습), batch32/10k | 0.008 | 홈에서 정지 | **idle 어트랙터**: 시연 초반 평균 2.8s 정지 구간을 충실히 재현. open-loop은 GT 완벽 추종(모델 정상) |
| v2 `gr00t_t1_trim` | + **idle 트리밍**(120s 제거) | 0.010 | 결단력 있는 뻗기, 그러나 **큐브 무관 pan -77 고정** | **모드 고정**: 파지 pan 분포[-77,+27] 중 왼쪽 지배 모드만 재생. 우측 시작 시 파지·운반·릴리즈 수행 → 시작상태가 모드를 바꾸는 우연이지 시각 그라운딩 아님(홈+우측 큐브 → 또 -85) |
| v3 `gr00t_t1_vis` | + `tune_visual`(vision 학습), batch4/10k | **0.041** | 무동작 | **학습부족**: 유효노출 1.6 epoch(v2의 1/8) |
| v4 `gr00t_t1_vis2` | batch4×**grad accum 8**=유효32, tune_visual | **0.009** (12.66ep, 4.0h) | 좌측 드리프트·정지·파지실패 (3회 시행) | 학습부족 해소(v1/v2와 동등)에도 **증상 재현** → §3.2에서 원인 규명 |

### 3.2 근본 원인 규명 — **covariate shift (누적 오차)** (2026-07-20)

v4(vis2)가 loss 0.009로 v1/v2와 동등한데도 증상이 재현되자, "vision freeze가 원인"이라는
가설이 **반증**됐다. 이후 서버에 관측을 직접 주입하는 오프라인 프로브 4종으로 원인을 좁혔다.

#### 3.2.1 요인 배제 (무엇이 원인이 **아닌가**)

| 검증 | 방법 | 결과 |
|---|---|---|
| 모델의 시각 그라운딩 | `probe_swap_images.py` — state 고정, **영상만 좌↔우 교체** | ✅ **존재**. 영상 효과 18.9° vs state 효과 1.9° (10배, 4쌍 일관) |
| 학습 분포에서의 추종 | `probe_training_obs.py` — 학습 프레임 주입 vs GT 액션 비교 | ✅ **정확**. 중앙 큐브 ep3/8/13/18 전 구간 GT 추종, 좌측 드리프트 없음 |
| 시작 자세 | `goto_home.py` 후 state를 학습 50ep 분포와 z-score 비교 | ❌ 원인 아님 (전 관절 \|z\|<0.4) |
| 조명/밝기 | `probe_brightness_causal.py` — 학습 프레임을 배포 밝기로 감광 | ❌ 원인 아님 (평균 효과 **+0.1°**) |
| 카메라 구도 이동 | 밝기 정규화 후 지형지물 비교(매트 좌측경계·흰면적) | ❌ 변화 없음 (경계 72 vs 74~80px) |
| 실행/클램프 버그 | `actions.csv`(명령) vs `chunks.csv`(결과) 대조 | ❌ 아님 — **모델이 직접** 좌측을 명령 (state -5.6 → 명령 -8.3) |
| RGB/BGR 뒤바뀜 | 로깅 경로 역추적(저장 영상에서 큐브가 빨강) | ❌ 아님 |

#### 3.2.2 원인 확정 — wrist 뷰 오프셋과 명령의 **역상관**

배포 실행(HORIZON=16) 전 구간에서 **wrist 뷰 속 큐브의 좌우 오프셋**과 **다음 청크의 pan 변화**를
대조한 결과:

| chunk | pan | wrist 큐브 오프셋 | 다음 Δpan | 판정 |
|---|---|---|---|---|
| 7 | -4.5 | +58px | -2.0 | ✗ 반대 |
| 9 | -10.1 | +140px | -3.2 | ✗ 반대 |
| 10 | -13.3 | +187px | -3.8 | ✗ 반대 |
| 12 | -22.1 | +267px | -5.1 | ✗ 반대 |

> **상관계수 = -0.86.** 큐브가 오른쪽으로 벗어날수록 모델은 **더 왼쪽**을 명령하며,
> **오차가 커질수록 이탈 속도도 빨라진다**(가속 발산).

학습 데이터에서 시연자는 큐브를 wrist 중심 근처(≈+59px)에 항상 유지했다. 배포에서 관측된
**+267px는 학습 분포에 존재하지 않는 상태**다. 즉 모델은 "어긋났을 때 되돌아오는 법"을
배운 적이 없고, 그 영역에서의 출력은 사실상 임의(=지배적 최좌측 모드로 낙하)다.

#### 3.2.3 결론과 재해석

**실패 원인은 모델·학습설정이 아니라 데이터의 성격이다** — 50ep 전부가 시연자가 정확히
수행한 **깨끗한 성공 시연**이라 교정(recovery) 예시가 0건이다. 청크 단위 불연속 실행 때문에
작은 이탈은 필연적으로 발생하는데, 그 순간 학습에 없는 상태로 진입해 발산한다.
행동복제의 고전적 실패인 **covariate shift / compounding error**([DAgger, Ross et al. 2011,
arXiv:1011.0686](https://arxiv.org/abs/1011.0686))에 정확히 부합한다.

이 관점에서 v1~v4의 모든 관측이 **하나의 원인**으로 통합 설명된다:

| 기존 해석 | 재해석 |
|---|---|
| v1 "idle 어트랙터" | 이탈 후 학습 분포 밖 → 정지 모드로 낙하 |
| v2 "모드 고정(pan -77)" | 이탈 후 지배적 최좌측 모드로 낙하 |
| v3 "학습부족" | (이건 실제로 학습부족이 맞음 — v4로 해소 확인) |
| v4 좌측 드리프트 | 동일 원인의 직접 관측 (역상관 -0.86) |
| 시행마다 실패 양상 상이 | 초기 이탈 방향·크기에 따라 낙하하는 모드가 달라짐 |

부수적으로 **HORIZON 8→16이 그리퍼 개방을 복구**시켰다(3.2→**49.1**, 학습 최대 49와 일치).
동기식 클라이언트는 8스텝(160ms) 실행 후 추론(~300ms) 동안 정지해 **약 65% 시간을 멈춰 있는데**,
16스텝으로 늘려 정지 비율을 낮추자 이탈이 줄어 궤적 후반(파지 단계)까지 도달했다.
이는 원인 진단과 정합하며, **실행 리듬도 이탈량에 기여**함을 보여준다.

### 3.3 진단 방법론 (재사용 자산)

- **open-loop 평가**(`eval_policy.py`): 학습 데이터 재생으로 모델/배포 문제 분리 — v1이
  GT를 완벽 추종함을 확인해 "모델은 정상, 배포 조건이 문제"로 조기 확정
- **관측 비교 실험**: 라이브 관측 vs 학습 첫프레임을 동일 zmq 서버에 주입 → 이미지 통계·
  state 일치, **둘 다 출발 명령 생성** 확인 (전처리 무결 입증)
- **클라이언트 계측**: rerun(카메라·state·action) + CSV(청크별 state/추론지연, 스텝별 액션)
  + 정책 시점 video.mp4 — "movement인지 진동인지 주차인지"를 수치로 판정
- **캘리브레이션 검증**: 7/10 재캘리브레이션 전후 좌표계 비교 — 신 캘리브레이션이 학습
  좌표계와 일치(구본은 wrist_roll -54° 어긋남 = 혼 슬립을 신본이 흡수). 홈 자세 손배치
  오차가 ±10°에 달해 **`goto_home.py`(학습 평균 자세 ±2° 복귀)를 측정 프로토콜에 도입**

### 3.2 인프라 (원격 추론)

- 서버: 5090 `inference_service.py`(zmq:5555), `gr00t-server.sh {t1|t2|logs|stop}`
- 클라이언트: 공식 `eval_lerobot.py`+`service.py`를 로컬로 가져와 패치
  (lerobot 0.6 모듈명, gr00t 미설치 로컬 대응 optional import, 계측 로깅, atexit 비디오 마무리)
- 측정 러너: `eval_gr00t_t1_trials.sh` — 시행마다 홈 복귀→배치 대기→60초 자동 실행
- 동기식 구조라 추론(~330ms)마다 정지가 끼는 주기 끊김은 구조적 (async 아님)

## 4. T2

- v3→v2 변환·stats 수정 완료, v1 학습 완료(loss 0.0105, 도중 checkpoint-7500 저장 행(hang)
  → 깨진 체크포인트 삭제 후 5000부터 resume으로 복구). **T1 판정 확정 후 동일 처방 적용 예정**

## 5. 현재 상태와 다음

**실기 T1은 정식 10회 측정에 진입하지 못했다** — §3.2에서 원인이 데이터 성격(교정 시연 부재)으로
확정되어, 같은 데이터로 하이퍼파라미터만 바꾸는 반복은 더 이상 정보 가치가 없다고 판단.
v1~v4 반복 진단 자체를 GR00T의 산출물로 기록하고 **sim-to-real 단계로 전환**한다(사용자 결정).

### 5.1 처방 (효과 순)

| # | 처방 | 근거 |
|---|---|---|
| 1 | **교정(recovery) 시연 데이터 추가** — 큐브에서 어긋난 자세에서 시작해 되돌아와 파지하는 10~20ep | §3.2가 지목한 근본 원인을 직접 겨냥. 기존 50ep 폐기 불필요(추가만) |
| 2 | **실행 이탈 축소** — async 추론(`policy_server`), 스텝 간격을 학습 fps와 일치(0.02→0.033s) | HORIZON 16 실험에서 이탈 감소 효과 실측(그리퍼 개방 복구) |
| 3 | co-training (자체 50ep + HF `youliangtan/so101-table-cleanup`) | 상태 분포 확장 — `dataset_path` List 지원 |

### 5.2 sim-to-real로 넘어가는 근거

이번 규명은 **sim 단계의 필요성을 강화한다**: 교정 데이터는 실기에서 손으로 모으기 번거롭고
(어긋난 자세를 일일이 만들어야 함) 재현성도 낮은데, 시뮬레이터에서는 **임의 초기자세로부터
자동·대량 생성**이 가능하다. 도메인 랜덤화(DR)까지 얹으면 상태 분포를 훨씬 넓게 덮을 수 있다.
→ [model_md/sim2real/04_GR00T_sim.md](../sim2real/04_GR00T_sim.md)

- T2는 v1 학습만 완료(loss 0.0105)한 상태로 보류 — 동일 원인이 예상되므로 실기 반복 대신
  sim 경로에서 처방 검증 후 재개

### 5.3 진단 도구 (본 규명에서 신규 작성, 재사용 가능)

`setup/gr00t/`에 추가 — π0/GR00T뿐 아니라 **ACT/SmolVLA 진단에도 이식 가능**:
- `probe_training_obs.py` — 학습 관측 주입 후 GT 액션과 대조 (state 분기 교란 통제 포함)
- `probe_swap_images.py` — state 고정·영상만 교체하는 통제 실험 (시각 사용 여부 판정)
- `probe_deploy_frame.py` — 배포 프레임 주입으로 실패 오프라인 재현 + 요인 제거
- `probe_deploy_timeseries.py` — 배포 전 구간 재주입으로 발산 시점 특정
- `probe_brightness_causal.py` — 조명 갭의 인과 검증

## 6. 시뮬레이터 선정 (차기 단계, 4모델 공통)

**Isaac Sim + Isaac Lab (공식 Sim-to-Real SO-101 Workshop + LeIsaac)** 확정:
- SO-101이 Isaac Sim **공식 내장 자산**, NVIDIA가 SO-101+Isaac Lab+GR00T 전 과정을 워크숍으로 제공
- LeIsaac teleop → **LeRobot 포맷 출력** = 4모델 전부 기존 파이프라인 재사용 가능 (결정적 이유)
- 배치: 양 PC Docker 설치(워크숍 공식이 Docker 기반, RTX 5090 테스트됨) — **경량(ACT/SmolVLA)
  → 5070Ti, 대형(π0/GR00T) → 5090** 분업. sim 이미지 양쪽 빌드 진행 중
- 로컬 Docker 설치 중 부수 이슈 해결: claude-desktop apt 저장소 GPG 만료(비활성화),
  systemd 미추적 잔존 sshd의 포트 22 점유로 openssh-server postinst 실패(정리 후 복구)

**Phase 0 완료 (2026-07-20)**: 양 PC 이미지 빌드·버전 동일 확인(Isaac Lab 2.3.2 / Isaac Sim
5.1.0-rc.19), 씬 로드 검증(로컬 GUI·5090 headless), 로컬 리더암 teleop 연결 검증까지 완료.
함정 3종(git-lfs USD 포인터, 필수 마운트 누락 시 무음 종료, calibration 디렉터리 네이밍)은
[setup/sim/README.md](../../setup/sim/README.md)에 기록.

---

*관련: [model_md/04_GR00T_N1.5.md](../04_GR00T_N1.5.md),
[setup/gr00t/](../../setup/gr00t/) (modality/변환/트리밍/측정 스크립트), [report/Pi0_report.md](Pi0_report.md)*
