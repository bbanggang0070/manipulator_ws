# 일반화 성능 평가 계획 — sim / 실기 3단계

> 학습이 끝난 모델들이 **학습 분포 밖**에서도 동작하는지를 축별로 측정한다.
> 지금까지의 평가는 "학습 분포 내 성공률"과 "위치·색상 OOD"에 한정돼 있었다.
>
> 작성 2026-08-11 · 관련: [보고서](README.md) · [co-training 계획](cotraining_plan_v4.md) · [배치표](real_collect_schedule.md)

---

## 0. 세 가지 평가

| | 대상 모델 | 환경 | 목적 |
|---|---|---|---|
| **A** | `cotrain_v4` | **실기** | **다음 레버 결정** — 노출 부족인가 커버리지 갭인가 |
| **B** | `v4_200` (sim-only) | **sim** | 물체·지시문·씬을 바꿔가며 **일반화 한계 측정** |
| **C** | `cotrain_v4` | **실기** | B에서 확인된 일반화가 **실기로 전이되는가** |

**A가 최우선**이다 — 학습 방향(스텝 ↑ / 데이터 추가 / mix 조정)이 여기서 갈리고,
그 결정이 B·C의 대상 모델을 바꾼다.

---

## 1. 공통 원칙 — 측정을 먼저 믿을 수 있게

이 프로젝트에서 **진단이 두 번 뒤집혔고 두 번 다 계측 문제**였다([보고서 §8](README.md)).
같은 실수를 반복하지 않기 위한 규칙이다.

### 1-1. 통제 실험 — 한 번에 한 축만

기준 조건을 고정하고 **한 축만** 바꾼다. 여러 축을 동시에 켜면 신호가 묻힌다
(1차 평가 설계가 이것 때문에 실패했다 — 모든 조건이 45%로 붙었다).

### 1-2. 분모를 먼저 검사한다

- **sim**: 블록이 박스·로봇팔에 가려 파지 불가능한 씬은 **분모에서 제외**한다.
  실측 발생률 **17.8%** — 빼지 않으면 어떤 비교도 무의미하다.
  `scenes.csv`의 `block_box_dist < 0.08`을 1차 후보로 잡고 **영상으로 확인**한다.
- **실기**: 물체가 그리퍼 사거리 밖이거나 박스가 화면 밖인 시행은 제외하고 사유를 기록한다.

### 1-3. 표본 수와 신뢰구간

| N | 95% CI 폭 (SR 70% 기준) |
|---|---|
| 10 | 약 ±25%p |
| 20 | ±19%p |
| **45** | **±13%p** |

**N=10에서 20%p 이내 차이는 노이즈다.** 두 번의 오진단이 모두 N=10~20에서 방향을 확정한 결과였다.

- **sim**: 무인이라 사람 비용이 0 → **조건당 45ep** (약 30분)
- **실기**: 사람이 붙어야 함 → **조건당 10회**, 차이가 애매하면 그 조건만 20회로 늘림

### 1-4. 기록

- sim: `scenes.csv`(좌표) + 영상. 판정은 `analyze_eval_scenes.py`로 구간 분해
- 실기: `LOGDIR`로 시행별 분리 — `video.mp4`, `actions.csv`, `chunks.csv`, rerun 실시간

### 1-5. 성공 판정

sim은 `vial_placed_on_rack` termination을 쓴다(2026-08-06에 `grasp_history_window`
20→1000으로 고쳐 오표기 0건 확인). **단, 물체 형상을 바꾸면 재검증이 필요하다**(§3-4 주의).
실기는 육안 판정한다.

---

## 2. 평가 A — 실기 진단 (배치표 기반)

> **co-training 완주 직후 가장 먼저.** 다음 학습 레버가 여기서 결정된다.

### 2-1. 설계

실기 데이터는 **박스 자세 10개 × 6ep**로 모았다([배치표](real_collect_schedule.md)).
학습에서 **본 자세**와 **못 본 자세**를 반반 시험해 결핍의 성격을 가른다.

| 조건 | 시행 | 박스 자세 |
|---|---|---|
| **In-dist** | 5회 | 배치표의 자세 **1, 3, 5, 7, 9** 를 그대로 재현 |
| **OOD** | 5회 | 배치표에 **없는** 자세 — 인접 자세의 **중간값** |

블록은 두 조건 모두 근접(8~18cm)과 원거리(20~30cm)를 섞는다.

#### OOD 자세 선정 — 화각 안전성 확인 필요

중간값은 기존 자세들의 범위 안이므로 **도달 불가 위험은 낮다**. 문제는 **카메라 화각**이다 —
수집 때 배치표의 각도 범위(±66°)를 sim에서 그대로 옮겼는데 실기 화각이 좁아
**P1·P3·P6·P9·P10에서 박스가 프레임 가장자리에 걸렸다**([datasets.md](datasets.md)).

→ **온전히 보였던 자세(P4·P5·P8, P7은 경계) 사이의 중간값**만 OOD로 쓴다.

| OOD 후보 | 기준 자세 | 박스 각도·거리 |
|---|---|---|
| O1 | P4(−26°, 31cm) ↔ P5(−7°, 29cm) 중간 | 약 −16°, 30cm |
| O2 | P5(−7°) ↔ P7(+19°, 32cm) 중간 | 약 +6°, 31cm |
| O3 | P7(+19°) ↔ P8(+39°, 29cm) 중간 | 약 +29°, 30cm |
| O4 | P4 ↔ P8 거리만 변경 | 각도 P5, 거리 **34cm**(범위 상단) |
| O5 | P5 ↔ P7 거리만 변경 | 각도 P7, 거리 **28cm**(범위 하단) |

**시행 전 절차**: 박스를 놓은 뒤 카메라 화면에서 **박스 전체가 프레임 안에 있는지** 확인한다.
잘려 있으면 각도를 줄여 다시 놓고, 그 사실을 기록한다.

```bash
# 카메라 확인 (추론 전, 로봇 불필요)
cd ~/manipulator_ws/envs/lerobot
uv run python ../../setup/gr00t/rerun_cam_align.py     # real/front 로 배치 확인
```

### 2-2. 판정과 처방

| 결과 | 해석 | 다음 레버 |
|---|---|---|
| **둘 다 실패 / collapse** | 실기 노출 부족 (underfit) | **스텝 ↑ (86k)** — 데이터 추가는 오히려 epoch을 깎아 역효과 |
| **In-dist 성공, OOD 실패** | **커버리지 갭** | 실기 **자세 추가 수집** + 스텝 동반 상향 (57→97ep면 82k 필요) |
| **둘 다 성공** | 문제 없음 | 평가 C로 진행 |
| 파지·접근 자체를 못 함 | **sim 스킬 부족** | 86k 재학습 (sim epoch 18.5 → 33.2) |

> **왜 이 구분이 중요한가**: 데이터를 늘리면 같은 스텝에서 실기 epoch이 **오히려 줄어든다**
> (57ep 40.0 → 97ep 23.5). "노출 부족"인데 데이터를 늘리면 악화된다.

### 2-3. 실행

```bash
# 5090
~/serve_blocktask_n16_5090.sh gr00t_blocktask_cotrain_v4_n16_8bit/checkpoint-48000 start

# 로컬 — 시행마다 LOGDIR을 바꾼다
cd ~/manipulator_ws/setup/gr00t
HOME_SCRIPT=goto_home_real.py \
LOGDIR=~/manipulator_ws/logs/evalA/indist_pose1_t1 \
STEP_DT=0.033 SERVER_HOST=192.168.0.56 ENSEMBLE=1 ENSEMBLE_W=0.3 ./home_then_infer.sh
```

> ⚠️ **`HOME_SCRIPT=goto_home_real.py`** — 실기 데이터가 섞인 모델은 실기 rest 자세로 시작해야 한다.
> zero-shot에서 쓰던 `goto_home_sim.py`와 다르다.

---

## 3. 평가 B — sim 일반화 (v4_200, sim-only)

> sim은 무인·재현 가능·ground-truth 판정이라 **넓게 훑기 좋다.**
> 여기서 어느 축이 약한지 알아내면, 실기에서는 그 축만 확인하면 된다.

### 3-1. 기준 조건

`full`(수집과 동일한 전체 랜덤) · 45ep · seed 고정. **현 성적 75.6%가 기준선**이다.

### 3-2. 축 1 — 지시문 (언어 일반화)

물체·씬은 그대로 두고 **문장만** 바꾼다. VLM 백본이 언어를 실제로 쓰는지, 아니면
시각만 보고 외운 행동을 내는지 가른다.

| # | 지시문 | 변형 |
|---|---|---|
| L0 | `Pick up the block and place it in the box` | **학습 문장 (기준)** |
| L1 | `Pick up the red block and place it in the box` | 속성 추가 |
| L2 | `Put the block into the box` | 축약·동사 변경 |
| L3 | `Grab the cube and put it in the container` | **동의어 전면 교체** |
| L4 | `Move the red cube into the black container` | 전면 교체 + 속성 |
| L5 | `Pick up the block and place it on the table` | **의미 충돌** (실제 태스크와 다른 목적지) |

- **L1~L4가 L0와 비슷하면** → 언어를 유연하게 해석함
- **L3~L4에서 크게 떨어지면** → 특정 토큰에 과적합

#### ⚠️ L5는 자동 판정을 쓸 수 없다 — 지표가 뒤집혀 있다

`vial_placed_on_rack`은 **"블록이 박스 안에 있는가"** 만 본다. 그런데 L5에서 **올바른 행동은
박스에 넣지 않는 것**이므로 자동 라벨의 의미가 반대이거나 모호해진다.

| 자동 판정 | 실제 의미 |
|---|---|
| `success` (박스 안) | **언어를 무시하고 외운 행동을 실행** → 나쁜 신호 |
| `fail` | **① 테이블에 놓음 = 언어 이해** / ② 박스 시도했다 실패 = 평범한 실패 — **구분 불가** |

→ **L5는 전 시행을 육안 3분류한다.**

| 분류 | 의미 |
|---|---|
| ㉠ 박스에 넣음 | 언어 무시 (시각만으로 행동) |
| ㉡ **파지 후 박스 밖에 릴리스** | **언어 반영** ← 이 비율이 언어 사용 지표 |
| ㉢ 파지 실패 | 판단 불가 (분모에서 제외) |

**언어 사용률 = ㉡ / (㉠ + ㉡)** 로 보고한다. 자동 SR은 참고로만 남긴다.

### 3-3. 축 2 — 물체 색상

이미 측정된 축이다(`color_blue` 70%, `color_green` 100%, N=10).
**N=45로 재측정**해 신뢰구간을 좁힌다. 추가로 노랑·흰색을 넣어 극단을 본다.

| 조건 | diffuse_color |
|---|---|
| `color_red` (기준) | (0.9, 0.1, 0.1) |
| `color_blue` | (0.1, 0.1, 0.9) |
| `color_green` | (0.15, 0.6, 0.15) |
| `color_yellow` | (0.9, 0.85, 0.1) |
| `color_white` | (0.9, 0.9, 0.9) ← **배경과 유사 → 검출 난이도 최대** |

### 3-4. 축 3 — 물체 형상·크기 (신규)

`block_base.spawn`이 `CuboidCfg`라 교체·조정이 가능하다.

| 조건 | 변경 | 의도 |
|---|---|---|
| `obj_cube` (기준) | 20mm 정육면체 | — |
| `obj_small` | **15mm** 정육면체 | 파지 정밀도 |
| `obj_large` | **28mm** 정육면체 | 그리퍼 개폐 한계 |
| `obj_sphere` | 반지름 10mm 구 | **형상 일반화** |
| `obj_cylinder` | 반지름 10 × 높이 20mm 원기둥 | 형상 일반화 |
| `obj_tall` | 20×20×35mm 직육면체 | 종횡비 |

> **⚠️ 성공 판정 재검증 필요**: 구·원기둥은 놓은 뒤 **굴러서 박스 경계를 벗어날 수 있다.**
> 판정 로직은 "그리퍼가 떨어진 뒤 경계 안"을 보므로 굴러 나가면 실패로 찍힌다.
> 형상 조건은 **영상으로 전수 확인**하고, 오표기가 많으면 그 조건만 육안 판정한다.
>
> 또 그리퍼 접촉 판정(`force_threshold=2`)이 작은 물체에서 안 걸릴 수 있다.
> `obj_small`은 파지했는데 미판정이 나오는지 먼저 5ep로 확인한다.

### 3-5. 축 4 — 씬 외형

| 조건 | 변경 | 비고 |
|---|---|---|
| `box_color` | 박스를 회색·갈색으로 | 현재 검은색 고정 |
| `mat_texture` | 테이블 매트 텍스처 변경 | 배경 일반화 |

> **조명은 제외한다.** `sky_light` 랜덤화가 렌더에 거의 도달하지 않음이 실측 확인됐다
> (라이트박스가 씬을 감쌈, ep별 밝기 175.9/180.3/176.8…로 동일).
> 조명을 정말 바꾸려면 `reset_lightbox_light_exposure` 범위를 건드려야 한다.

### 3-5b. 축 5 — 시각적 방해물 (distractor)

작업과 **무관한 물체**를 함께 두고, 모델이 타깃에 집중하는지 본다.
"빨간 큐브를 박스에" 라는 태스크에서 다른 물체가 주의를 끄는지가 핵심이다.

| 조건 | 추가 물체 | 의도 |
|---|---|---|
| `dist_none` (기준) | 없음 | — |
| `dist_blue_cube` | 파란 큐브 1개 | 같은 형상·다른 색 |
| `dist_green_cyl` | 초록 원기둥 1개 | 다른 형상·다른 색 |
| `dist_two` | 파란 큐브 + 초록 원기둥 | 방해물 2개 |

**방해물 배치**: 타깃 블록에서 8cm 이상, 박스와도 겹치지 않게. 도달 범위 안에 둔다
(시야 밖이면 방해가 되지 않아 시험이 성립하지 않는다).

> **⚠️ 방해물 색에서 빨강 계열은 금지한다.** 성공 판정은 `vials=["block_red"]`만 추적하므로
> 모델이 방해물을 집어 박스에 넣어도 성공으로 안 잡히는 것은 의도대로다. 그러나
> **분석 스크립트가 빨간 픽셀로 타깃을 찾기 때문에**(`analyze_dataset_geometry.py`,
> `analyze_eval_scenes.py` 계열) 빨간 방해물은 **모델뿐 아니라 계측까지 교란**한다.

**판정**: 자동 SR만으로는 "엉뚱한 물체를 집음"과 "아무것도 못 집음"이 구분되지 않는다.
실패 영상은 **3분류**로 육안 확인한다 — `방해물을 집음` / `타깃을 집었으나 실패` / `접근 실패`.
첫 번째가 많으면 **시각적 grounding 실패**다.

### 3-6. 실행

```bash
# 조건별 45ep, 무인. 조건당 약 30분
LANG_INSTRUCTION="Grab the cube and put it in the container" \
MODEL=gr00t_blocktask_v4_200_n16_8bit/checkpoint-86000 \
  ~/blocktask_headless_scenes.sh full 45 31

# 분석
python3 setup/sim/t1_task/analyze_eval_scenes.py --label "L3" \
  --exclude "<겹침 ep>" inf_video/05_genA/hl_full_s31
```

### 3-7. 우선순위 (전부 돌리면 21조건 × 30분 = 10시간)

| 순위 | 조건 | 근거 |
|---|---|---|
| 1 | **L0, L3, L5** (3개) | 언어를 쓰는지 여부가 가장 큰 정보. L5는 육안 3분류 |
| 2 | **`dist_blue_cube`** (1개) | 시각적 grounding — 실기에서도 재현하기 쉬움 |
| 3 | `obj_sphere`, `obj_small` (2개) | 형상 일반화의 양 극단 |
| 4 | `color_white` (1개) | 검출 난이도 최대 |
| 5 | 나머지 | 위에서 신호가 보인 축만 채움 |
| — | 물리 극단치(§5-B) | 최하 — 실기로 전이되지 않음 |

**1~4순위 7조건 = 3.5시간**이면 어느 축이 약한지 윤곽이 잡힌다.

---

## 4. 평가 C — co-train 모델의 실기 일반화

> B에서 찾은 약축이 **실기에서도 재현되는지** 본다. 실기는 시행 비용이 크므로
> **B에서 유의한 차이가 나온 축만** 가져온다.

### 4-1. 조건 (B 결과에 따라 확정)

| 축 | 실기 조건 | 준비물 |
|---|---|---|
| 지시문 | L0 / L3 / L5 | 없음 — `LANG_INSTRUCTION` 환경변수 |
| 물체 | 기준 큐브 / 다른 물체 2종 | **실물 준비 필요** |
| 박스 | In-dist / OOD 자세 | 평가 A와 통합 |

**물체 후보**: 로봇 매니퓰레이션 벤치마크에서 흔히 쓰이는 것으로 고른다 —
YCB 세트류(작은 블록, 주사위, 스펀지 큐브) 또는 크기·색이 다른 큐브.
sim에 없는 물체일수록 일반화 시험이 되지만, **너무 다르면 태스크 자체가 달라진다**
(예: 원통형은 그리퍼 파지 방식이 다름). **크기·색 변형부터** 시작하는 것을 권한다.

### 4-2. 시행 수

조건당 **10회**. B에서 큰 차이(20%p 이상)가 나온 축은 20회로 늘린다.

### 4-3. 실행

```bash
cd ~/manipulator_ws/setup/gr00t
LANG_INSTRUCTION="Grab the cube and put it in the container" \
HOME_SCRIPT=goto_home_real.py \
LOGDIR=~/manipulator_ws/logs/evalC/L3_t1 \
STEP_DT=0.033 SERVER_HOST=192.168.0.56 ENSEMBLE=1 ENSEMBLE_W=0.3 ./home_then_infer.sh
```

---

## 5. 필요한 구현

| # | 항목 | 내용 | 비용 |
|---|---|---|---|
| 1 | **지시문 파라미터화** | 아래 ⚠️ 참조 — 단순 셸 변수가 아니라 **`-e` 전달**로 | 10분 |
| 2 | **물체 프리셋** | `configure_scene.py`에 형상·크기 프리셋 추가 (`CuboidCfg` → `SphereCfg`/`CylinderCfg`, size 변경) | 30분 |
| 3 | **색상 확장** | `COLOR` 딕셔너리에 yellow·white 추가 | 5분 |
| 4 | **박스·매트 색상** | `paper_box_base.spawn.visual_material`, 매트 텍스처 프리셋 | 20분 |
| 5 | **판정 재검증** | 구·원기둥에서 성공 판정이 오표기하는지 5ep로 확인 | 10분 |

> 구현은 **전부 `configure_scene.py` 프리셋 확장**으로 흡수한다. 씬 파일을 직접 고치는 방식은
> 드리프트를 만든 전례가 있어(5090이 `ref` 상태로 굳음) 프리셋 경유를 유지한다.

#### ⚠️ 지시문은 반드시 `-e` 로 넘긴다

현재는 바깥 셸이 값을 치환해 `bash -c` 문자열에 박아 넣는다:

```bash
bash -c "lerobot_eval ... --lang_instruction '$LANG' ..."
```

**컨테이너에 전달은 되지만 따옴표 중첩이 깨지기 쉽다.** 이 프로젝트에서 정확히 이 패턴 때문에
수집 스크립트의 언어 지시문이 **`"Pick"` 으로 절단**된 사고가 있었다.
평가 B는 지시문을 계속 바꾸는 작업이고 아포스트로피(`robot's`)가 섞이면 바로 깨진다.

→ 수집 스크립트에 적용한 **검증된 패턴**을 그대로 쓴다.

```bash
docker run ... -e LANG_INSTRUCTION="$LANG_INSTRUCTION" ... \
  bash -c "lerobot_eval ... --lang_instruction \"\$LANG_INSTRUCTION\" ..."
```

**적용 후 확인**: 아포스트로피가 든 문장으로 1ep 돌려 `scenes.csv`/로그에 전문이 남는지 본다.

---

## 5-B. 부록 축 — 물리 극단치 (우선순위 최하)

학습 DR 범위를 **크게 벗어난** 물리값에서 동역학적 강건함을 본다.

| 조건 | 학습 범위 | 극단값 |
|---|---|---|
| `phys_heavy` | 질량 ×0.5~1.5 | **×5** (5g → 25g) |
| `phys_slippery` | 마찰 static 0.56~1.04 | **×0.1** (0.8 → 0.08) |

### 유보를 권하는 이유

1. **풀 수 없는 조건이면 측정이 무의미하다.** 마찰 0.08은 사람이 텔레옵으로도 못 잡을 수 있다.
   그러면 SR 0%는 **정책이 아니라 물리를 잰 것**이고, 겹침 씬을 분모에서 뺐던 것과 같은 오류가 된다.
   → **해결 가능성 검증이 게이트다**: 해당 조건에서 텔레옵으로 5회 시도해 **최소 3회 성공**해야
   유효한 평가 조건으로 인정한다. 미달이면 배율을 낮춰 재설정한다.
2. **실기로 전이되지 않는다.** 실기 마찰·질량은 고정이라 sim 결과를 실기에서 확인할 수단이 없다.
   다른 축과 달리 **평가 C로 이어지지 못한다.**
3. **이미 약한 신호가 나왔다.** 학습 범위 내 `phys_dr`은 ref 대비 −20%p로 노이즈 범위였다.
4. **"미끄러짐 복구"는 구조적으로 제한된다.** `action_horizon=16`이라 정책은 **16스텝을
   open-loop로 실행한 뒤** 다시 관측한다. 즉각적인 슬립 감지·대응이 되는 구조가 아니다.
   이 축에서 낮게 나와도 "정책이 나쁘다"가 아니라 **"청크 실행 구조의 한계"** 일 수 있다.

### 그래도 가치가 있는 경우

**"모델이 폐루프 제어를 하는가, 궤적을 재생하는가"** 를 가르는 진단으로는 유효하다.
`action_horizon`을 8·4로 낮춰 함께 측정하면 구조 한계와 정책 한계를 분리할 수 있다.
다만 이는 **본 평가가 끝난 뒤의 심화 항목**으로 둔다.

---

## 6. 실행 순서

```
[지금]  co-training 48k 완주 대기                      (약 12h)
   ↓
[1] 평가 A — 실기 진단 10회                            (1h)  ← 최우선, 학습 레버 결정
   ↓
   ├ "둘 다 실패" → 86k 재학습 (65h) 후 A 재실행
   ├ "OOD만 실패" → 실기 수집 + 82k 재학습
   └ "둘 다 성공" → 아래로
   ↓
[2] 구현 (지시문·물체 프리셋)                          (1h)
   ↓
[3] 평가 B — sim 일반화 1~3순위 6조건                  (3h, 무인)
   ↓
[4] 평가 B 결과로 약축 특정 → 필요 조건만 추가          (0~7h)
   ↓
[5] 평가 C — 실기에서 약축 재현 확인                    (2~3h)
```

**평가 B는 co-training과 무관**하므로(대상이 `v4_200`) 5090이 비는 시간에 언제든 돌릴 수 있다.
다만 GPU를 쓰므로 재학습과는 겹칠 수 없다.

---

## 7. 결과 기록

각 평가의 결과는 아래 형식으로 이 문서에 누적한다.

| 평가 | 조건 | N | 제외 | SR | 95% CI | 비고 |
|---|---|---|---|---|---|---|
| B | L0 (기준) | 45 | | | | |
| B | L3 (동의어) | 45 | | | | |
| B | L5 (의미 충돌) | 45 | | | | ← 높으면 나쁜 신호 |

**영상**: `inf_video/05_generalization/<평가>_<조건>/`
**분석**: `analyze_eval_scenes.py --label <조건>` 출력을 그대로 붙인다.
