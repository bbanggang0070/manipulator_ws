# Python 가상환경 (uv.lock으로 재생성: cd envs/lerobot && uv sync)
envs/*/.venv/
__pycache__/
*.pyc

# 학습 산출물 — 체크포인트는 HF Hub(heongyu/act_so101_t1 등)에 보관
envs/*/outputs/

# 로그·일시 파일
logs/
nohup.out

# 카메라 점검 스냅샷
setup/hardware/snapshots/

# T1 sim2real 실기 추론 실험 산출물(대용량 영상·로그) — repo 제외
manipulator_md/sim2real/08_T1_sim2real/assets/generalization/

# 추론 영상(대용량) — repo 제외
inf_video/

# 오케스트레이터 실행 로그(로컬 전용)
.orchestrate.log
